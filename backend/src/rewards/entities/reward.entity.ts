/**
 * Canonical XP event log is {@link RewardHistory}.
 * `Reward` is an alias for API clarity and legacy imports.
 */
export { RewardHistory as Reward } from './reward-history.entity';
export { XpRewardReason } from './reward-history.entity';
